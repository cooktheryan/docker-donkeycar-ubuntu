version: '3'
services: 
  car:
    container_name: donkeycar
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    network_mode: "host"
    command: ["file1.py", "file2.py"]